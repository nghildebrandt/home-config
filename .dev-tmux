#!/usr/bin/env bash

dev-tmux() {
  if [ -n "$1" ]; then
    cd $1
  fi

  tmux new-session -d 'vim' -c $(pwd)
  tmux split-window -v -l 15 -d -c $(pwd)
  tmux split-window -h -l 50 -d -c $(pwd) -t ':.+'
  vim
}
